-- Бренд товара (уже читается скрапером из данных магазина, но раньше не
-- сохранялся) — нужен, чтобы сопоставление одинаковых товаров между
-- магазинами (match_products.py) не путало разные бренды с похожими
-- описаниями (см. обсуждение в чате: "Alma" vs "POLS" с одинаковым вкусом).
alter table store_products add column if not exists brand text;
